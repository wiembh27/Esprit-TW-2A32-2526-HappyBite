# HappyBite — variables d'environnement (optionnel sous XAMPP)
# Copiez ce fichier en .env à la racine OU configurez config/secrets.php

# Base de données MySQL
DB_HOST=localhost
DB_NAME=happybite
DB_USER=root
DB_PASS=

# Clés API (optionnel — fonctionnalités IA désactivées si vides)
OPENAI_API_KEY=
GEMINI_API_KEY=
